[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

# Project metadata (name, version, dependencies, entry points, etc.) is defined
# in setup.py, which computes the version and long description dynamically.
